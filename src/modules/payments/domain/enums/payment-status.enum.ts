export enum PaymentStatus {
    Pending = 'pending',
    Succeeded = 'succeeded',
    SessionCreated = 'session_created',
    Processing = 'processing',
    Authorized = 'authorized',
    Failed = 'failed',
    Cancelled = 'cancelled',
    Refunded = 'refunded',
    Disputed = 'disputed',
}
